
name: snake test nine

on:
  schedule:
    # every 6 hours
    - cron: "0 */6 * * *"

jobs:
  generate:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: Platane/snk@master
        id: snake-gif
        with:
          github_user_name: skema1114
          svg_out_path: dist/github-contribution-grid-snake.svg
